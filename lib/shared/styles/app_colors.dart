import 'package:flutter/material.dart';

/// Centralised color palette for AiTutor UI elements.
class AppColors {
  AppColors._();

  static const Color background = Color(0xFFEAF4F8);
  static const Color surface = Colors.white;

  static const Color primary = Color(0xFF0F5A63);
  static const Color primaryDark = Color(0xFF235A6E);
  static const Color accent = Color(0xFF0F5A63);

  static const Color textPrimary = Color(0xFF3F4A59);
  static const Color textMuted = Color(0xFF8FA2AA);

  static const Color iconMuted = Color(0xFF7D8A96);

  static const Color border = Color(0xFFCAD6DC);
  static const Color checkboxBorder = Color(0xFF0F5A63);

  static const Color shadow = Color(0x140F5A63);
}
